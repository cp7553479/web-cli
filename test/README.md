# test 目录说明

本目录用于手工/半自动脚本验证 CLI 行为（非 `npm test` 单元测试）。

**建议每次只跑一个脚本**，便于对照 **`<cwd>/.web/logs/`** 里的请求与响应排障。

## 快速对照表

| 场景 | 建议 |
|------|------|
| 项目覆写 | 在临时目录建 `.web/config.toml` 片段，结合日志与 `web config list` 核对合并来源 |
