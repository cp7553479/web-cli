# =============================================================================
# Web CLI 全局配置模板（TOML）
# =============================================================================
#
# 【各厂商 API Key 申请地址】（复制到浏览器打开）
#   Tavily       https://app.tavily.com/
#   Brave        https://brave.com/search/api/
#   Jina AI      https://jina.ai/  （官网「Reader / Search API」与控制台创建密钥）
#   Moonshot Kimi https://platform.moonshot.cn/
#   Firecrawl    https://www.firecrawl.dev/
#   Perplexity   https://www.perplexity.ai/settings/api
#   Gemini       https://aistudio.google.com/apikey
#   DuckDuckGo  Instant Answer 无单独 API Key（本模板 answer 段 ddg 可不配密钥）
#
# 【account 与账号 id】
#   表头形如 [search.account.perplexity-main]：最后一段为内部「账号 id」，可自定义；
#   同一 provider 可多行不同账号 id，按文件顺序依次尝试。
#
# 【与 .env】
#   api_token = "{$XXX}" 对应 ~/.web/.env 中响应的环境变量。
#   未写 enabled 时默认启用并解析占位符；enabled = false 时不解析、不要求变量非空。
#
# =============================================================================

# -----------------------------------------------------------------------------
# [search] — 网页搜索（各 [search.account.*] 按出现顺序尝试，*为内部识别的account id，与API无关）
# -----------------------------------------------------------------------------

[search]

[search.account.tavily-main]
provider = "tavily"
api_token = "{$TAVILY_API_KEY}"
enabled = true

# [search.account.brave-main]
# provider = "brave"
# api_token = "{$BRAVE_API_TOKEN}"
# enabled = true

# [search.account.jina-main]
# provider = "jina"
# api_token = "{$JINA_API_KEY}"
# enabled = true

# [search.account.firecrawl-main]
# provider = "firecrawl"
# api_token = "{$FIRECRAWL_API_KEY}"
# enabled = true

# [search.account.kimi-main]
# provider = "kimi"
# api_token = "{$MOONSHOT_API_KEY}"
# enabled = true

# [search.account.perplexity-main]
# provider = "perplexity"
# api_token = "{$PERPLEXITY_API_KEY}"
# enabled = true

# -----------------------------------------------------------------------------
# [fetch] — 按 URL 抓取（各 [fetch.account.*] 按出现顺序尝试，*为内部识别的account id，与API无关）
# -----------------------------------------------------------------------------

[fetch]

[fetch.account.jina-reader]
provider = "jina"
api_token = "{$JINA_API_KEY}"
enabled = true

# [fetch.account.firecrawl-scrape]
# provider = "firecrawl"
# api_token = "{$FIRECRAWL_API_KEY}"
# enabled = true

# [fetch.account.tavily-extract]
# provider = "tavily"
# api_token = "{$TAVILY_API_KEY}"
# enabled = true

# [fetch.account.kimi-fetch]
# provider = "kimi"
# api_token = "{$MOONSHOT_API_KEY}"
# enabled = true

# html2markdown：内置 HTML→Markdown 引擎（无需密钥），默认开启。
[fetch.account.html2markdown-main]
provider = "html2markdown"
enabled = true

# playwright：无头浏览器（需已安装 Playwright；无需密钥）。
[fetch.account.playwright-main]
provider = "playwright"
enabled = true

# http：直连 GET，默认关闭；需要时改为 enabled = true。
[fetch.account.http-main]
provider = "http"
enabled = false

# -----------------------------------------------------------------------------
# [research] — 仅官方 research API（允许的 provider：tavily、perplexity）
# -----------------------------------------------------------------------------

[research]

# [research.account.tavily-main]
# provider = "tavily"
# api_token = "{$TAVILY_API_KEY}"
# enabled = true

# Perplexity：Sonar chat/completions，默认 deep research 模型（可用 --vendor model=... 覆盖）
# [research.account.perplexity-main]
# provider = "perplexity"
# api_token = "{$PERPLEXITY_API_KEY}"
# enabled = true

# -----------------------------------------------------------------------------
# [answer] — 快答（各 [answer.account.*] 按出现顺序尝试，*为内部识别的account id，与API无关）
# -----------------------------------------------------------------------------

[answer]

[answer.account.ddg-main]
provider = "duckduckgo"
enabled = true

# [answer.account.brave-answer]
# provider = "brave"
# api_token = "{$BRAVE_API_TOKEN}"
# enabled = true

# [answer.account.gemini-main]
# provider = "gemini"
# api_token = "{$GEMINI_API_KEY}"
# enabled = true

# -----------------------------------------------------------------------------
# [runtime]
# -----------------------------------------------------------------------------

[runtime]
logging = true
